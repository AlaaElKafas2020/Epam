
To build the project using Ant:
ant -f build.xml

To build the project using Maven:
mvn clean package

To build the project using Gradle:
gradle clean build

To run the tests using Ant:
ant -f build.xml test

To run the tests using Maven:
mvn test

